# Fresh attention/head per candidate; fixed ESM inputs and EIHR homology priors.
# Both baselines exclude 2VAU-A and 5LSQ-A only from PDB validation.
# One scalar z starts at 0; alpha_h=sigmoid(z).
# Scalar AdamW group: gate LR multiplier, weight_decay=0; no-hit keeps global fusion.
batch_size: 16
seed: 42
resources: configs/fusion_baseline_resources.yaml
num_trials: 20
trial_epochs: 8
top_k_params: 5
patience: 10
final_epochs: 150
base_dir_search: runs/sequence_homology_fixed_fusion/validation_exclude_two_v1/search
base_dir_final: runs/sequence_homology_fixed_fusion/validation_exclude_two_v1/final
use_search_results: true
search_results_path: null
search_space:
  learning_rate: [0.0001, 0.0003, 0.001]
  gate_lr_multiplier: [0.05, 0.1, 0.2]
  weight_decay: [0.00001, 0.0001, 0.001]
  attn_hidden_dim: [128, 256, 512]
  attn_dropout: [0.0, 0.1, 0.2, 0.3]
  head_hidden_dim: [512]
  head_dropout: [0.0, 0.1, 0.2, 0.3]
  lambda_hier: [0.0, 0.001, 0.01]
  pos_weight_cap: [5.0, 10.0, 20.0]
